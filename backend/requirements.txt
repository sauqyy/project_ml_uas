# --- Core web & database ---
Flask>=3.0.0
flask-cors>=4.0.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
psycopg2-binary>=2.9.0   # driver PostgreSQL (produksi/Render); tak dipakai kalau SQLite lokal

# --- Machine Learning ---
pandas>=2.0.0
numpy>=1.20.0
scikit-learn>=1.0.0
statsmodels>=0.13.0
prophet>=1.1.0
joblib>=1.2.0

# --- Telegram bot & AI parsing (opsional, hanya untuk fitur bot Telegram) ---
pyTelegramBotAPI>=4.12.0
google-generativeai>=0.5.0

# --- OCR struk (opsional, hanya untuk fitur foto struk di Telegram) ---
# easyocr menarik PyTorch + torchvision. Di Render/Linux, install torch+torchvision CPU DULU,
# lalu KUNCI versinya pakai constraint supaya langkah `pip install -r` ini tidak menimpanya
# dengan torchvision PyPI yang mismatch (penyebab error "torchvision::nms does not exist"):
#   pip install --no-cache-dir torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip freeze | grep -iE "^(torch|torchvision)==" > /tmp/torch-constraint.txt
#   pip install -c /tmp/torch-constraint.txt -r backend/requirements.txt
easyocr>=1.7.0
Pillow>=9.0.0

# --- Production server (opsional) ---
gunicorn>=21.0.0
